apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.application.name }} {{- include "helper.labels" . | nindent 2 }}
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
  storageClassName: nfs-client