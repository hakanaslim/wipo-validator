apiVersion: v1
kind: PersistentVolumeClaim
metadata:
name: {{ include "application.name" . }}-{{ .Release.Namespace }}
      {{- include "helper.labels" . Release.Namespace  }}
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 4Gi
  storageClassName: nfs-client