kind: Service
apiVersion: v1
metadata:
  name: biot-svs-svc
spec:
  selector:
    app: "{{ .Values.application.name }}"
    component: "{{ .Values.application.name }}"
    instance: "{{ .Values.instance }}"
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080