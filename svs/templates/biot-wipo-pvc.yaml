apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: "{{.Release.Namespace}}-{{.Chart.Name}}-pvc"
    labels:
        app: biot-wipo
        cleanup: "{{.Release.Namespace}}-{{.Chart.Name}}-pvc"
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client